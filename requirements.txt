# Telegram bot
python-telegram-bot==20.3

# Solana SDK (no py-serde required)
solders==0.18.0

# Compatible async + networking stack
httpx==0.24.1
websockets==12.0
aiohttp==3.9.5
asyncio==3.4.3

# Environment config
python-dotenv==1.0.1

# Optional logging
loguru==0.7.2

# For API calls (e.g. Jupiter or swap APIs)
requests==2.31.0
