# Telegram bot (stable + compatible with Python 3.12)
python-telegram-bot==20.3

# Solana dev (solders works fine without py-serde directly)
solders==0.18.0

# Async and networking
httpx==0.27.0
websockets==12.0
aiohttp==3.9.5
asyncio==3.4.3

# .env support
python-dotenv==1.0.1

# Logging (optional but useful)
loguru==0.7.2

# If using Jupiter or external API calls
requests==2.31.0
